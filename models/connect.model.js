var sql = require('mssql/msnodesqlv8');
var config = require('../config/dataConfig')

// const conn = new sql.ConnectionPool(config).connect().then(pool => {return pool});

// module.exports = {
//     conn: conn,
//     sql: sql
// }